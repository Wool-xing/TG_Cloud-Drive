# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Environment Variables
.env
.env.local
.env.*.local

# Node.js dependencies — never commit (gigabytes, machine-specific)
node_modules/

# Build outputs
backend/dist/
frontend/dist/

# Cloudflare Wrangler local state (contains preview tokens, KV cache)
.wrangler/

# Personal IDE / agent config (machine-specific paths)
.mcp.json

# TLS certs — generated locally, never committed
certs/*.pem
certs/*.key

# Data
data/*.json
!data/*.json.example
chunks/*
!chunks/.gitkeep
uploads/*
!uploads/.gitkeep
logs/*.log
!logs/.gitkeep

# User and sensitive data
my_tg_app/TGpan/user.json
my_tg_app/TGpan/files.json
secrets/
*.pem
*.key

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/

# Security
secrets/
*.key
*.pem

# Temporary files
*.tmp
*.bak
*.swp
*.log.*

# OS
Thumbs.db
.DS_Store

# 内部协作文档（仅本地保留，不公开）
项目协作宪章.md
代码审查报告.md
灵感笔记.md

# quickstart 备份的旧 .env
.env.backup.*
.env.audit-backup.*

# 自签 TLS 证书（每次本地生成，不入仓）
certs/fullchain.pem
certs/privkey.pem
certs/acme-webroot/
